{
  "name": "register",
  "version": "1.0.0",
  "description": "",
  "main": "register.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start": "cd dist && node register.js",
    "bns": "npm run build && npm run start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "fs": "^0.0.1-security",
    "playwright": "^1.20.2"
  },
  "devDependencies": {
    "typescript": "^4.6.3"
  }
}
